<h1>MixMasters Premier Audio Services</h1>
<h3>Welcome <%= @current_user.username %></h3>
<dl>
    <% @services.each do |service| %>
        <dt>$<%= service.price %> <%= link_to service.title, service %> (<%= link_to "@"+service.user.username, service.user %>)</dt>
        <dd>-<%= service.description %></dd><br>
    <% end %>

</dl>

